{ ... }:

# ============================================================
# Docker Aliases Aggregator
# ============================================================
# Imports all Docker-related alias modules
# ============================================================

{
  imports = [
    ./build.nix
    ./run.nix
    ./compose.nix
  ];
}
