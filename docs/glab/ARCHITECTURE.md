# GitLab Duo Nix Configuration - Architecture

## 🏗️ Arquitetura

```
┌─────────────────────────────────────────────────────────────────┐
│                     Seu Projeto (Root)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  flake.nix (Seu flake principal)                         │  │
│  │  - Pode integrar GitLab Duo como input                   │  │
│  │  - Ou usar separadamente                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  nix/ (GitLab Duo - Desacoplado)                         │  │
│  │  ├── flake.nix                                           │  │
│  │  ├── gitlab-duo/                                         │  │
│  │  │   ├── settings.yaml (Configuração)                   │  │
│  │  │   ├── module.nix (Módulo Nix)                        │  │
│  │  │   └── default.nix (Package)                          │  │
│  │  ├── modules/                                            │  │
│  │  ├── scripts/                                            │  │
│  │  ├── examples/                                           │  │
│  │  └── README.md                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo de Configuração

```
┌─────────────────────────────────────────────────────────────────┐
│  1. Setup Inicial                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  mkdir -p ~/.config/gitlab-duo                                 │
│  echo "api_key" > ~/.config/gitlab-duo/api-key                │
│  chmod 600 ~/.config/gitlab-duo/api-key                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  2. Entrar no Ambiente Nix                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  nix develop ./nix                                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  3. Carregar Configuração (shellHook)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  nix/flake.nix shellHook:                                      │
│  ├── Exporta variáveis de ambiente                             │
│  ├── Carrega API key de ~/.config/gitlab-duo/api-key          │
│  ├── Cria diretórios necessários                               │
│  └── Exibe mensagem de sucesso                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  4. Validar Configuração                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  bash nix/scripts/validate-gitlab-duo.sh                       │
│                                                                 │
│  Verifica:                                                      │
│  ✓ Variáveis de ambiente                                       │
│  ✓ Feature flags                                               │
│  ✓ Configuração de modelos                                     │
│  ✓ Rate limiting                                               │
│  ✓ Cache                                                        │
│  ✓ Logging                                                      │
│  ✓ Arquivos de configuração                                    │
│  ✓ Sintaxe YAML                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  5. Usar GitLab Duo                                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Variáveis disponíveis:                                        │
│  - GITLAB_DUO_ENABLED                                          │
│  - GITLAB_DUO_ENDPOINT                                         │
│  - GITLAB_DUO_FEATURES_*                                       │
│  - GITLAB_DUO_MODEL_*                                          │
│  - GITLAB_DUO_RATE_LIMIT_*                                     │
│  - GITLAB_DUO_CACHE_*                                          │
│  - GITLAB_DUO_LOG_*                                            │
│  - GITLAB_DUO_API_KEY                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Estrutura de Configuração

```
nix/gitlab-duo/settings.yaml
│
├── duo.enabled
│   └── true
│
├── duo.api
│   ├── endpoint
│   ├── timeout
│   ├── retries
│   └── backoff_factor
│
├── duo.features
│   ├── code_completion
│   ├── code_review
│   ├── security_scanning
│   └── documentation_generation
│
├── duo.models
│   ├── code_generation
│   ├── code_review
│   └── security
│
├── duo.rate_limit
│   ├── requests_per_minute
│   ├── tokens_per_minute
│   └── burst_size
│
├── duo.cache
│   ├── enabled
│   ├── backend
│   ├── ttl_seconds
│   └── max_size_mb
│
├── duo.logging
│   ├── level
│   ├── format
│   ├── output
│   └── file_path
│
├── duo.security
│   ├── verify_ssl
│   ├── allowed_domains
│   └── sanitize_output
│
├── duo.performance
│   ├── async_processing
│   ├── worker_threads
│   └── queue_size
│
└── duo.project
    ├── id
    ├── name
    ├── language
    └── framework
```

## 🔐 Segurança

```
┌─────────────────────────────────────────────────────────────────┐
│  Configuração Pública (Versionada)                              │
├─────────────────────────────────────────────────────────────────┤
│  nix/gitlab-duo/settings.yaml                                  │
│  nix/flake.nix                                                 │
│  nix/gitlab-duo/module.nix                                     │
│  (Sem secrets)                                                  │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│  Configuração Privada (Não Versionada)                          │
├─────────────────────────────────────────────────────────────────┤
│  ~/.config/gitlab-duo/api-key                                  │
│  (Permissões: 600)                                              │
│  (Carregada dinamicamente no shellHook)                         │
└─────────────────────────────────────────────────────────────────┘
```

## 🔗 Integração com Seu Flake

### Opção 1: Usar Separadamente (Atual)

```
seu-projeto/
├── flake.nix (seu flake principal)
└── nix/ (GitLab Duo)
    └── flake.nix

Uso: nix develop ./nix
```

### Opção 2: Integrar como Input

```
seu-projeto/
├── flake.nix (com input gitlab-duo)
└── nix/ (GitLab Duo)
    └── flake.nix

Uso: nix develop
```

### Opção 3: Usar Módulo Nix

```
seu-projeto/
├── flake.nix (com imports)
└── nix/ (GitLab Duo)
    └── gitlab-duo/module.nix

Uso: nix develop
```

## 📈 Escalabilidade

```
┌─────────────────────────────────────────────────────────────────┐
│  Projeto 1                                                      │
│  └── nix develop ./nix                                          │
├─────────────────────────────────────────────────────────────────┤
│  Projeto 2                                                      │
│  └── nix develop ./nix                                          │
├─────────────────────────────────────────────────────────────────┤
│  Projeto 3                                                      │
│  └── nix develop ./nix                                          │
└─────────────────────────────────────────────────────────────────┘

Cada projeto pode:
✓ Usar a mesma configuração
✓ Customizar independentemente
✓ Manter suas próprias versões
✓ Compartilhar boas práticas
```

## 🎯 Benefícios da Arquitetura

| Aspecto | Benefício |
|---------|-----------|
| **Desacoplamento** | GitLab Duo independente do projeto |
| **Versionamento** | Rastreável no Git |
| **Reproducibilidade** | Mesma config em todos os ambientes |
| **Segurança** | Secrets não versionados |
| **Flexibilidade** | Fácil de customizar |
| **Reutilização** | Pode ser usado em múltiplos projetos |
| **Manutenção** | Atualizações sem afetar projeto |
| **Clareza** | Separação clara de responsabilidades |
